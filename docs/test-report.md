# テストレポート（プレースホルダ）

このファイルには family-electricity-cost-share アプリのテスト結果や確認事項を記録していきます。

手動テスト

### ログイン画面表示
実施：2026-03-17

- [x] `/login` にアクセスしてログイン画面が表示される  
- [x] テンプレートエラー（500エラー）が発生しない  
- [x] モックどおりのレイアウト・デザインで表示される（見た目が崩れていない）  
- [x] `login_id` 入力欄が表示される  
- [x] `password` 入力欄が表示される  
- [x] ログインボタンが表示される  
- [x] `form` タグがあり `method="POST"` が設定されている  
- [x] CSRFトークンが埋め込まれている（`{{ csrf_token() }}`）
  
- [ ] `login_id` と `password` を両方空欄で送信すると「ログインIDまたはパスワードが違います」が表示される
- [ ] `login_id` だけ入力して送信すると「ログインIDまたはパスワードが違います」が表示される
- [ ] `password` だけ入力して送信すると「ログインIDまたはパスワードが違います」が表示される
- [ ] ログイン失敗時、エラーメッセージが常に同じ文言で表示される
- [ ] ログイン失敗後も `login_id` の入力値が保持される
- [ ] ログイン失敗後、`password` は再表示されない
- [ ] 存在しない `login_id` で送信してもサーバーエラーにならず、同じエラーメッセージが表示される
- [ ] POST送信時にCSRFエラーが発生しない（通常操作でそのまま送信できる）

- [ ] まだユーザー未作成のため、ログイン成功テストは未実施として保留にする
