# テストレポート（プレースホルダ）

このファイルには family-electricity-cost-share アプリのテスト結果や確認事項を記録していきます。

手動テスト

### ログイン画面表示・ログイン処理

実施：2026-03-16
- [x] `/login` にアクセスしてログイン画面が表示される  
- [x] テンプレートエラー（500エラー）が発生しない  
- [x] モックどおりのレイアウト・デザインで表示される（見た目が崩れていない）  
- [x] `login_id` 入力欄が表示される  
- [x] `password` 入力欄が表示される  
- [x] ログインボタンが表示される  
- [x] `form` タグがあり `method="POST"` が設定されている  
- [x] CSRFトークンが埋め込まれている（`{{ csrf_token() }}`）

実施：2026-03-17  
- [x] `login_id` と `password` を両方空欄で送信すると「ログインIDまたはパスワードが違います」が表示される
- [x] `login_id` だけ入力して送信すると「ログインIDまたはパスワードが違います」が表示される
- [x] `password` だけ入力して送信すると「ログインIDまたはパスワードが違います」が表示される
- [x] ログイン失敗時、エラーメッセージが常に同じ文言で表示される
- [x] ログイン失敗後も `login_id` の入力値が保持される
- [x] ログイン失敗後、`password` は再表示されない
- [x] 存在しない `login_id` で送信してもサーバーエラーにならず、同じエラーメッセージが表示される
- [x] POST送信時にCSRFエラーが発生しない（通常操作でそのまま送信できる）

実施：2026-03-21
- [x] まだユーザー未作成のため、ログイン成功テストは未実施として保留にする

-------------------------

### Step2-3 実装確認
実施：2026-03-17

- [x] `redirect_by_role()` を追加した
- [x] `/login` の GET 時に `current_user.is_authenticated` を判定している
- [x] ログイン成功時に `redirect_by_role(user)` を呼んでいる
- [x] `role == "admin"` の遷移先を定義している
- [x] `role == "user"` の遷移先を定義している
- [x] 想定外の `role` の場合、flashメッセージを出して `/login` に戻す処理がある

### Step2-3 手動テスト（roleによる遷移分岐）
実施：2026-03-17

- [x] （ユーザー作成後に実施）admin ユーザーでログインすると管理者側トップへ遷移する
- [x] （ユーザー作成後に実施）user ユーザーでログインするとユーザー側トップへ遷移する

実施：2026-03-21
- [x] （ユーザー作成後に実施）ログイン済みの admin が `/login` にアクセスすると管理者側トップへ戻る
- [x] （ユーザー作成後に実施）ログイン済みの user が `/login` にアクセスするとユーザー側トップへ戻る

-------------------------

### 【修正後に追加】認証まわり確認（権限制御・キャッシュ対策・文言修正）
実施：2026-03-18

- [x] 未ログインで `/user/top` にアクセスすると、ログイン画面へ遷移して「ログインしてください」が表示される
- [x] 未ログインで `/admin/top` にアクセスすると、ログイン画面へ遷移して「ログインしてください」が表示される
- [x] 未ログインで `/admin/users` にアクセスすると、ログイン画面へ遷移して「ログインしてください」が表示される

- [x] user で `/admin/top` にアクセスすると、フラッシュメッセージなしで `/user/top` に戻る
- [x] user で `/admin/users` にアクセスすると、フラッシュメッセージなしで `/user/top` に戻る
- [x] admin で `/user/top` にアクセスすると、フラッシュメッセージなしで `/admin/top` に戻る

- [x] user でログアウト後、ブラウザの戻るボタンを押してもログイン後画面が見えにくくなっている
- [x] admin でログアウト後、ブラウザの戻るボタンを押してもログイン後画面が見えにくくなっている
- [x] （参考）ログアウト後、戻るボタンで一時的に画面が見えても、再操作時には未ログイン扱いでログイン画面へ遷移する

-------------------------

### Step3 手動テスト（ログアウト）
実施：2026-03-17

- [x] POSTで `/logout` にアクセスすると `/login` にリダイレクトされる

-------------------------

### /user/top /admin/top 実装確認
実施：2026-03-17

- [x] `/user/top` ルートを追加した
- [x] `/admin/top` ルートを追加した
- [x] `/user/top` に `login_required` を付けている
- [x] `/admin/top` に `login_required` を付けている
- [x] `/admin/top` に `admin_required` を付けている
- [x] 一般ユーザーが管理者画面へ来たときの制御がある
- [x] ユーザー用トップ画面が `user_top.html` を表示する
- [x] 管理者用トップ画面が `admin_top.html` を表示する
- [x] ユーザー用トップ画面の右上の鍵アイコンが `/logout` の POST に接続されている
- [x] 管理者用トップ画面のログアウトが `/logout` の POST に接続されている
- [x] CSRFトークンがログアウトフォームに埋め込まれている

### Step4 手動テスト
実施：2026-03-17

- [x] user でログインして `/user/top` が表示される
- [x] admin でログインして `/admin/top` が表示される
- [x] 未ログインで `/user/top` にアクセスすると `/login` に戻る
- [x] 未ログインで `/admin/top` にアクセスすると `/login` に戻る
- [x] user で `/admin/top` にアクセスすると、自分側へ戻される
- [x] ユーザー用トップ画面のデザインがモックどおりに表示される
- [x] 管理者用トップ画面のデザインがモックどおりに表示される
- [x] ユーザー用トップ画面の右上の鍵アイコンが表示崩れしていない
- [x] 管理者用トップ画面のログアウトボタンが表示される
- [x] ログアウト実行で `/login` に戻る

-------------------------

### Step5 実装確認
実施：2026-03-17

- [x] ユーザー用トップ画面の右上の鍵アイコンが、直接送信ではなくモーダル起動ボタンになっている
- [x] モーダル内に `POST /logout` のフォームがある
- [x] CSRFトークンがモーダル内のログアウトフォームに埋め込まれている
- [x] 「ログアウトする」ボタンで送信できる構造になっている
- [x] 「やめる」ボタンで閉じる処理がある
- [x] モーダル外クリックで閉じる処理がある
- [x] 管理者用トップ画面にはモーダルを追加していない

### Step5 手動テスト（ユーザー用ログアウト確認モーダル）
実施：2026-03-17

- [x] user でログインして `/user/top` を開く
- [x] 右上の鍵アイコンが表示崩れしていない
- [x] 鍵アイコンを押すとログアウト確認モーダルが表示される
- [x] モーダルの見た目がモックどおりに近い
- [x] 「やめる」を押すとモーダルが閉じる
- [x] モーダル外の暗い部分を押すとモーダルが閉じる
- [x] 「ログアウトする」を押すとログアウトされて `/login` に戻る
- [x] ログアウト後、`/user/top` に再アクセスすると `/login` に戻される

-------------------------

### Step1 実装確認・手動テスト（管理者：ユーザー管理画面）
実施：2026-03-17

- [x] `/admin/users` ルートを追加している
- [x] `/admin/users` に `login_required` を付けている
- [x] `/admin/users` に `admin_required` を付けている
- [x] ユーザー一覧を created_at, id の昇順で取得している
- [x] `admin_users.html` に users 一覧を埋め込む `for` 文がある
- [x] 役割を「管理者 / メンバー」で表示している
- [x] 左メニューの `HOME` が管理者トップ画面へリンクしている
- [x] 左メニューの `シェアメンバー` がユーザー管理画面へリンクしている
- [x] 左メニューのログアウトが `POST /logout` になっている

- [x] admin でログインして `/admin/users` が表示される
- [x] 一覧に既存ユーザーが表示される
- [x] 未ログインで `/admin/users` にアクセスすると `/login` に戻される
- [x] user 権限で `/admin/users` にアクセスすると拒否されてユーザー側トップにリダイレクトする
- [x] 左メニューの `HOME` を押すと管理者トップ画面へ戻る
- [x] 左メニューの `シェアメンバー` を押しても表示崩れなく同画面に遷移できる
- [x] 左メニューのログアウトで `/login` に戻る
- [x] 画面デザインがモックどおりに大きく崩れていない

-------------------------

### Step2 実装確認・手動テスト（管理者：ユーザー新規登録）
実施：2026-03-18

- [x] `/admin/users` が `GET, POST` に対応している
- [x] 管理者のみアクセス可能なまま維持されている
- [x] 新規登録フォームに `action` と `method="post"` が設定されている
- [x] CSRFトークンが埋め込まれている
- [x] `name` を受け取っている
- [x] `login_id` を受け取っている
- [x] `password` を受け取っている
- [x] `role` を受け取っている
- [x] `theme_color` を受け取っている
- [x] 空欄チェックがある
- [x] `role` を `user/admin` のみに制限している
- [x] テーマカラー選択値をカラーコードへ変換して保存する実装がある
- [x] `login_id` の重複チェックがある
- [x] パスワードを `generate_password_hash` でハッシュ化して保存している
- [x] 登録成功後に `/admin/users` へリダイレクトしている
- [x] 入力値の差し戻しがある

- [x] 管理者で `/admin/users` を開いてフォームが表示される
- [x] どこかを未入力、またはすべて未入力で送信するとエラーメッセージが表示される
- [x] `role` はラジオボタンで必ず選択されるため、UI上で未選択状態は発生しない
- [x] テーマカラーは色丸のラジオボタンで必ず選択されるため、UI上で未選択状態は発生しない
- [x] `role` はサーバー側バリデーションとDB制約で不正値を防御している
- [x] テーマカラーはサーバー側バリデーションで不正値を防御している
- [x] 既存の `login_id` で登録しようとすると重複エラーが表示される
- [x] 正しい値で送信すると新規ユーザーが登録される
- [x] 登録後、一覧に新しいユーザーが表示される
- [x] 登録したユーザーの role が一覧に正しく表示される
- [x] 登録したユーザーの color がDBにカラーコードで保存されている
- [x] 画面デザインが大きく崩れていない

-------------------------

### Step2 実装確認・手動テスト（管理者：ユーザー削除）
実施：2026-03-18

- [x] （実装確認）存在しないユーザーID指定時は安全に一覧へ戻る処理がある
- [x] （実装確認）一般ユーザーは /admin/users にアクセスできないため、削除処理にも到達できない

- [x] 管理者のみ /admin/users にアクセスできる
- [x] 一覧画面にユーザーが表示される
- [x] 削除ボタン押下で確認モーダルが表示される
- [x] モーダルに対象ユーザー名・IDが正しく表示される
- [x] 「削除する」で POST リクエストが送信される
- [x] CSRFトークンが含まれている
- [x] 削除成功時に一覧から対象ユーザーが消える
- [x] 削除成功時にflashメッセージが表示される
- [x] ログイン中ユーザー自身は削除できない
- [x] 存在しないユーザーID指定時も安全に一覧へ戻る
- [x] 一般ユーザーは削除処理にアクセスできない

- [ ] 運転中機器があるユーザーは削除できない

-------------------------

## ■ 【修正後に追加】ユーザー削除安全対策
実施：2026-03-18

### 【実装確認】
- [x] 自分削除禁止のチェックがある
- [x] 所有機器がある場合の削除禁止チェックがある
- [x] 運転中チェックが残っている
- [x] チェック順序が「自分 → 所有機器 → 運転中」になっている
- [x] 削除不可時に flash が出る実装になっている

---

### 【手動テスト】
- [x] 機器を持っていないユーザーは削除できる
- [x] 自分自身は削除できない
- [x] 機器を1台でも持っているユーザーは削除できない
- [x] 「登録済み機器があるメンバーは削除できません。」が表示される
- [x] 一般ユーザーで削除URLにアクセスしても削除できない
- [x] 未ログインで削除URLにアクセスするとログイン画面へ遷移する

-------------------------

## ■ 【仕様変更後に追加】ユーザー削除安全対策（削除方針統一）
実施：2026-03-19

### 【実装確認】
- [x] finalized_bill_members がある場合の削除禁止チェックが追加されている
- [x] チェック順序が「自分 → 所有機器 → 確定データ → 運転中」になっている
- [x] broad な except Exception が削除されている
- [x] 削除可否が事前チェックのみで判定されている

---

### 【手動テスト】
- [x] 条件をすべて満たすユーザーは削除できる

-------------------------

## ■ 実装確認 & 手動テスト（管理者：機器管理画面）
実施：2026-03-18

### 【画面表示】
- [x] /admin/devices にアクセスできる
- [x] ログイン済みでないと /login にリダイレクトされる
- [x] 一般ユーザーでアクセスすると user_top にリダイレクトされる
- [x] 管理者でアクセスすると画面が表示される

### 【一覧表示】
実施：2026-03-21
- [x] devices テーブルの内容が表示される
- [x] 機器名が表示される
- [x] 使用メンバー名（device.user.name）が表示される
- [x] 消費電力が表示される

### 【UI】
- [x] モックのレイアウトが崩れていない
- [x] サイドバーリンクが動作する
- [x] 削除ボタンが表示されている（まだ動作不要）

### 【エラー確認】
- [x] joinedload の import エラーが出ない

-------------------------

## ■ 実装確認 & 手動テスト（Step2: 管理者 機器新規登録）
実施：2026-03-18

### 【実装確認】
- [x] /admin/devices が GET / POST の両方に対応している
- [x] 管理者のみアクセスできる
- [x] 一覧表示と新規登録を同一画面で行う実装になっている
- [x] 使用メンバーのプルダウンが users テーブル連動になっている
- [x] user_id を整数変換し、存在する users.id のみ受け付ける実装になっている
- [x] power_kw を Decimal で数値チェックしている
- [x] power_kw が 0 以下なら登録不可になっている
- [x] 色丸の選択値をカラーコードへ変換して保存する実装になっている
- [x] POST フォームに CSRF トークンが入っている
- [x] バリデーションエラー時に入力値の差し戻しがある
- [x] app.py に `from sqlalchemy.orm import joinedload` が入っていることを確認する

### 【手動テスト】
- [x] 管理者で /admin/devices を開くと機器管理画面が表示される
- [x] 一般ユーザーで /admin/devices を開くと拒否される
- [x] 未ログインで /admin/devices を開くとログイン画面へ遷移する
- [x] 使用メンバーのプルダウンに users テーブルの name が表示される
- [x] 正常値（機器名、既存ユーザー、正の数値、色選択）で登録できる
- [x] 登録成功後、/admin/devices にリダイレクトされる
- [x] 登録した機器が一覧に表示される
- [x] 機器名が空欄だと登録できない
- [x] 使用メンバー未選択だと登録できない
- [x] 消費電力が空欄だと登録できない
- [x] 消費電力に文字列（例: abc）を入れると登録できない
- [x] 消費電力に負数（例: -1）を入れると登録できない
- [x] 消費電力に 0 を入れると登録できない
- [x] バリデーションエラー時に入力済みの値が保持される
- [x] 正常登録時、devices テーブルに color がカラーコードで保存される

-------------------------

## 【修正後に追加】実装確認 & 手動テスト（一般ユーザーのみ選択制御を追加後）
実施：2026-03-18

### 【実装確認】
- [x] 使用メンバー一覧取得が role='user' のみになっている
- [x] POST時に target_user.role == "user" のチェックが入っている
- [x] 存在しない user_id の場合もエラーになる
- [x] flash("使用メンバーの選択が不正です。") が出る実装になっている

### 【手動テスト】
- [x] 使用メンバーのプルダウンに管理者が表示されない
- [x] 一般ユーザーのみ表示される
- [x] 一般ユーザーを選択して機器登録できる
- [x] 登録後、一覧に正しく表示される
- [x] 登録した機器の使用メンバーが正しく表示される

-------------------------

## ■ 実装確認 & 手動テスト（Step3: 管理者 機器削除）
実施：2026-03-18

### 【実装確認】
- [x] /admin/devices/<int:device_id>/delete の POST ルートが追加されている
- [x] 管理者のみアクセス可能になっている
- [x] 削除対象機器の存在確認が入っている
- [x] end_time が NULL の利用記録で「運転中」を判定している
- [x] 運転中の機器は flash を出して削除不可になっている
- [x] （旧仕様）削除処理で IntegrityError を捕捉してロールバックしている
- [x] 削除ボタンからモーダルへ device名 / user名 / delete URL を渡している
- [x] 実削除は hidden form の POST + CSRF トークン送信になっている
- [x] モーダル開閉用の JavaScript は最小限になっている

### 【手動テスト】
- [x] 管理者で機器一覧を開き、各行の削除ボタンで確認モーダルが開く
- [x] モーダルに対象の機器名と使用メンバー名が正しく表示される
- [x] モーダルの「キャンセル」で閉じる
- [x] モーダル外クリックで閉じる
- [x] モーダルの「削除する」で POST 送信される
- [x] 削除URLは GET では実行できず、405 になる

- [x] （旧仕様）過去ログがある機器でも削除できる
- [x] （旧仕様）削除後、その機器の usage_log がDBから消えている

-------------------------

## ■ 【仕様変更後に追加】機器削除安全対策（削除方針統一）
実施：2026-03-19

### 【実装確認】
- [x] device_usage_logs が存在する場合の削除禁止チェックが追加されている
- [x] device_usage_logs を先に削除する処理が削除されている
- [x] broad な except Exception が削除されている
- [x] 削除処理が delete → commit のみになっている

---

### 【手動テスト】
- [x] 使用記録がない機器は削除できる
- [x] 使用記録がある機器は削除できない
- [x] 「使用記録がある機器は削除できません。」が表示される

-------------------------

## ■ 一般ユーザートップ画面 Step1
実施：2026-03-20

### 【実装確認】
- [x] /user/top で、運転中レコードあり / なし に応じてテンプレート出し分けしている
- [x] 一般ユーザー以外は user 画面からリダイレクトされる
- [x] ログイン中ユーザー名をテンプレートに渡している
- [x] ログイン中ユーザーの所有機器だけを取得している
- [x] 自分の所有機器に紐づく運転中レコードだけを確認している
- [x] 運転中でないトップに所有機器一覧を表示している
- [x] 既存のログアウト確認モーダルを維持している
- [x] 運転中トップにログアウトボタンを設置していない

---

### 【手動テスト】
- [x] 一般ユーザーでログインすると /user/top が表示される
- [x] 運転中レコードがない場合、通常トップ（user_top_idle.html）が表示される
- [x] 通常トップにログイン中ユーザー名が表示される
- [x] 通常トップに自分の所有機器だけが表示される
- [x] 鍵アイコン押下でログアウト確認モーダルが開く
- [x] モーダルの「やめる」で閉じる
- [x] モーダルの「ログアウトする」でログアウトできる

-------------------------

## ■ 一般ユーザートップ画面 Step2
実施：2026-03-20

### 【実装確認】
- [x] 運転開始用の POST ルートが追加されている
- [x] 一般ユーザー以外は開始処理からリダイレクトされる
- [x] device_id の不正値チェックがある
- [x] ログイン中ユーザーの所有機器だけ開始対象にしている
- [x] 二重開始防止のサーバー側チェックがある
- [x] 運転開始時に device_usage_logs を作成している
- [x] 開始成功時に success flash を出さない方針になっている
- [x] idleトップ、runningトップともに異常系flash表示枠を維持している
- [x] runningトップにログアウトボタンを設置していない

---

### 【手動テスト】
- [x] idleトップで自分の機器ボタンを押すと runningトップへ切り替わる
- [x] runningトップにログアウトボタンが表示されない
- [x] 開始成功時に success flash が表示されない
- [x] 停止後、Neon の device_usage_logs で対象レコードの start_time に値が入る

-------------------------

## ■ 一般ユーザートップ画面 Step3
実施：2026-03-20

### 【実装確認】
- [x] 運転停止用の POST ルートが追加されている
- [x] 停止対象を URL 指定ではなく、サーバー側で「自分の運転中1件」から取得している
- [x] ログイン中ユーザーの所有機器に紐づく運転中レコードだけを停止対象にしている
- [x] 二重停止対策として、停止対象がない場合の異常系flashがある
- [x] runningトップに現在運転中の機器名を表示している
- [x] 停止ボタンが POST + CSRF トークン送信になっている

---

### 【手動テスト】
- [x] runningトップで「運転を停止」を押すと idleトップへ戻る
- [x] 停止後、runningトップではなく idleトップが表示される
- [x] runningトップに現在運転中の機器名が表示される
- [x] runningトップの運転中の機器名の背景の図形が該当機器のテーマカラーになっている
- [x] 停止後、Neon の device_usage_logs で対象レコードの end_time に値が入る

-------------------------

## ■ 一般ユーザートップ画面 Step4
実施：2026-03-20

### 【実装確認】
- [x] runningトップで app_settings.estimated_unit_price を取得している
- [x] runningトップに運転開始時刻を渡している
- [x] runningトップに機器の消費電力を渡している
- [x] 経過時間を初回表示時に計算する実装がある
- [x] 経過時間を1分ごとに自動更新する実装がある
- [x] 料金目安 = 消費電力 × 経過時間 × 仮単価 で計算している
- [x] 仮単価未設定時は「仮単価未設定」と表示する分岐がある

---

### 【手動テスト】
- [x] runningトップで経過時間が表示される
- [x] 1分後に経過時間表示が更新される
- [x] runningトップで料金目安が表示される

-------------------------

## ■ 一般ユーザートップ画面 開始/停止確認モーダル
実施：2026-03-20

### 【実装確認】
- [x] idleトップに運転開始確認モーダルが追加されている
- [x] idleトップの開始ボタンは直接送信せず、確認モーダルを開く実装になっている
- [x] 開始確認モーダルの「開始する」で対応する開始フォームを送信する実装になっている
- [x] runningトップに運転停止確認モーダルが追加されている
- [x] runningトップの停止ボタンは直接送信せず、確認モーダルを開く実装になっている
- [x] 停止確認モーダルの「停止して記録を追加」で停止フォームを送信する実装になっている
- [x] idleトップの既存ログアウト確認モーダルを維持している
- [x] runningトップにログアウトボタンを追加していない

---

### 【手動テスト】
- [x] idleトップで機器ボタンを押すと運転開始確認モーダルが開く
- [x] 開始確認モーダルの「やめる」で閉じる
- [x] 開始確認モーダルの「開始する」で runningトップへ切り替わる
- [x] idleトップでログアウト確認モーダルがこれまでどおり開く
- [x] runningトップで「運転を停止」を押すと停止確認モーダルが開く
- [x] 停止確認モーダルの「やめる」で閉じる
- [x] 停止確認モーダルの「記録を終了」で idleトップへ戻る

---

## ■ 一般ユーザートップ画面 最終確認
実施：2026-03-21

### 【手動テスト】
- [x] 一般ユーザーでログインすると、自分のトップ画面が表示される
- [x] idleトップで機器ボタンを押すと、開始確認モーダルが開く
- [x] 開始確認モーダルの「開始する」で runningトップへ切り替わる
- [x] runningトップに運転中の機器名が表示される
- [x] runningトップで「運転を停止」を押すと、停止確認モーダルが開く
- [x] 停止確認モーダルの「停止して記録を追加」で idleトップへ戻る

-------------------------

## ■ 一般ユーザー：記録新規作成 Step 1
実施：2026-03-21

### 【実装確認】
- [x] `/user/usage/new` のGETルートがある
- [x] `/user/usage/logs` のGETルートがある
- [x] 両ルートに `login_required` がある
- [x] admin がアクセスした場合にロール別トップへ戻すチェックがある
- [x] 新規追加画面でログイン中ユーザー所有の機器だけを取得している
- [x] 新規追加画面テンプレートが `devices` を受け取って機器選択肢を描画している
- [x] 新規追加画面の家アイコンからトップへ戻る導線がある
- [x] 新規追加画面の「やめる」からトップへ戻る導線がある
- [x] 記録一覧画面テンプレートが追加されている
- [x] 記録一覧画面にflash表示枠がある
- [x] 記録一覧画面の家アイコンからトップへ戻る導線がある

---

### 【手動テスト】
- [x] userで `/user/usage/new` にアクセスすると新規追加画面が表示される
- [x] userで `/user/usage/logs` にアクセスすると記録一覧画面が表示される
- [x] 未ログインで `/user/usage/new` にアクセスするとログイン画面へ遷移して「ログインしてください」が表示される
- [x] 未ログインで `/user/usage/logs` にアクセスするとログイン画面へ遷移して「ログインしてください」が表示される
- [x] adminで `/user/usage/new` にアクセスするとフラッシュメッセージなしで `/admin/top` に戻る
- [x] adminで `/user/usage/logs` にアクセスするとフラッシュメッセージなしで `/admin/top` に戻る
- [x] 新規追加画面の機器プルダウンに自分の機器だけが表示される
- [x] 新規追加画面の家アイコンを押すと `/user/top` に戻る
- [x] 新規追加画面の「やめる」を押すと `/user/top` に戻る
- [x] 記録一覧画面の家アイコンを押すと `/user/top` に戻る


-------------------------

## ■ 一般ユーザー：記録新規作成 Step 2（日時統一・最終版）
実施：2026-03-21

### 【実装確認】
- [x] `/user/usage/new` が GET/POST 両対応になっている
- [x] `login_required` が設定されている
- [x] admin がアクセスした場合にロール別トップへリダイレクトされる
- [x] フォームから device_id / start_time / end_time を受け取っている
- [x] device_id が数値変換されている
- [x] 選択機器がログインユーザーの所有機器であることをチェックしている
- [x] `datetime.fromisoformat()` による日時変換処理がある
- [x] `ZoneInfo("Asia/Tokyo")` を使って日本時間として解釈している
- [x] JST → UTC 変換処理（astimezone）がある
- [x] `.replace(tzinfo=timezone.utc)` を使っていない
- [x] 自動記録の開始/停止で `datetime.now(timezone.utc)` を使っている
- [x] 手動入力・自動記録ともにUTC aware datetimeで統一されている
- [x] DB定義（TIMESTAMP WITH TIME ZONE）と整合している
- [x] 不正日時入力時にエラーハンドリングがある
- [x] `DeviceUsageLog` に保存処理がある
- [x] DB保存失敗時に rollback がある
- [x] 保存成功時に success flash が表示される
- [x] 保存成功時に `/user/usage/logs` にリダイレクトされる
- [x] 入力エラー時に新規追加画面へ再表示される
- [x] 入力値の差し戻し（form_data）が実装されている
- [x] CSRFトークンがフォームに含まれている

---

### 【手動テスト】
- [x] userで新規追加画面を開ける
- [x] 正常入力で「追加する」を押すと記録一覧画面へ遷移する
- [x] 追加後に「新しい記録を追加しました」が表示される
- [x] 機器を選ばずに送信するとエラーになる
- [x] start_time に不正な文字列を入れるとエラーになる
- [x] 入力エラー時、入力値が保持されている

記録一覧画面を実装後にテスト
実施：2026-03-
- [ ] 日本時間で入力した「10:00」が、アプリ内でズレずに扱われる（9時間ズレない）
- [ ] 運転開始→停止で作られた記録と、手動追加の記録で時間のズレがない

-------------------------

## ■ 一般ユーザー：記録新規作成 Step 3 バリデーション
実施：2026-03-21

### 【実装確認】
- [x] 使用機器の必須チェックがある
- [x] 運転開始日時の必須チェックがある
- [x] 選択機器IDの数値化チェックがある
- [x] 選択機器が存在するかのチェックがある
- [x] 選択機器がログイン中ユーザー所有かのチェックがある
- [x] 運転開始日時の形式チェックがある
- [x] 運転停止日時の形式チェックがある
- [x] 運転開始日時の未来日時チェックがある
- [x] 運転停止日時の未来日時チェックがある
- [x] 停止日時入力時のみ `start_time < end_time` のチェックがある
- [x] 論理削除済みを除外した未終了記録の存在チェックがある
- [x] 未終了記録がある場合、停止日時なしの追加を禁止している
- [x] バリデーションエラー時に danger flash を表示している
- [x] エラー時に新規追加画面を再表示している

---

### 【手動テスト】
- [x] 使用機器を未選択で送信するとエラーメッセージが表示される
- [x] 運転開始日時を未入力で送信するとエラーメッセージが表示される
- [x] 運転開始日時に未来の日時を入れるとエラーメッセージが表示される
- [x] 運転停止日時に未来の日時を入れるとエラーメッセージが表示される
- [x] 運転停止日時が運転開始日時以前だとエラーメッセージが表示される
- [x] 既に未終了記録がある状態で、停止日時を空欄のまま追加するとエラーメッセージが表示される
- [x] 既に未終了記録がある状態でも、停止日時ありの過去記録は追加できる
- [x] エラー時に入力値が保持されている
- [x] 正常入力なら新しい記録を追加できる

-------------------------

## ■ 一般ユーザー：トップ画面からの導線接続
実施：2026-03-21

### 【実装確認】
- [x] 運転中でないトップ画面から「忘れた分を追加」で新規追加画面へ遷移する導線がある
- [x] 運転中でないトップ画面から「記録をみる」で記録一覧画面へ遷移する導線がある
- [x] 運転中トップ画面から「忘れた分を追加」で新規追加画面へ遷移する導線がある
- [x] 運転中トップ画面から「記録をみる」で記録一覧画面へ遷移する導線がある
- [x] 新規追加画面でカテゴリ付きflash表示に対応している
- [x] 記録一覧画面でカテゴリ付きflash表示に対応している
- [ ] トップ画面の家アイコンからトップへ戻る導線がある

---

### 【手動テスト】
- [x] 運転中でないトップ画面で「忘れた分を追加」を押すと新規追加画面へ遷移する
- [x] 運転中でないトップ画面で「記録をみる」を押すと記録一覧画面へ遷移する
- [x] 運転中トップ画面で「忘れた分を追加」を押すと新規追加画面へ遷移する
- [x] 運転中トップ画面で「記録をみる」を押すと記録一覧画面へ遷移する
- [x] 新規追加画面でバリデーションエラー時に赤系のflashが表示される
- [x] 新規追加成功後、記録一覧画面で成功flashが表示される

-------------------------

## ■ ルート統一（/ → ロール別トップリダイレクト）
実施：2026-03-21

### 【実装確認】
- [x] `/` にアクセスしたときのビュー関数が存在する
- [x] 未ログイン時に `/` からログイン画面へリダイレクトされる処理がある
- [x] ログイン済みユーザーをロールに応じてトップ画面へリダイレクトする処理がある
- [x] 一般ユーザーUIの家アイコンの遷移先が `/` に変更されている
- [x] 管理者UIのロゴ（家アイコン＋タイトル）の遷移先が `/` に変更されている

---

### 【手動テスト】
- [x] 未ログイン状態で `/` にアクセスするとログイン画面に遷移する
- [x] 一般ユーザーでログイン後、`/` にアクセスすると `/user/top` に遷移する
- [x] 管理者でログイン後、`/` にアクセスすると `/admin/top` に遷移する
- [x] 一般ユーザー画面の家アイコンを押すとトップ画面に戻る（状態に応じて表示が崩れない）
- [x] 管理者画面のロゴを押すと管理者トップ画面に戻る


-------------------------

## ■ 一般ユーザー：使用記録一覧 Step 1
実施：2026-03-22

### 【実装確認】
- [x] `/user/usage/logs` のGETルートがある
- [x] `/user/usage/<int:usage_log_id>/edit` のGETルートがある
- [x] `/user/usage/<int:usage_log_id>/delete` のGETルートがある
- [x] `/user/share-amounts` のGETルートがある
- [x] 上記ルートに `login_required` がある
- [x] admin がアクセスした場合にロール別トップへ戻すチェックがある
- [x] 使用記録一覧画面の家アイコンが `/` に接続されている
- [x] 使用記録一覧画面にカテゴリ付きflash表示枠がある
- [x] 編集削除選択モーダルのHTML土台がある
- [x] モーダル内に編集画面への導線がある
- [x] モーダル内に削除画面への導線がある
- [x] 編集画面テンプレートに家アイコンで `/` へ戻る導線がある
- [x] 削除画面テンプレートに家アイコンで `/` へ戻る導線がある
- [x] シェア金額一覧画面テンプレートに家アイコンで `/` へ戻る導線がある

---

### 【手動テスト】
- [x] userで `/user/usage/logs` にアクセスすると使用記録一覧画面が表示される
- [x] userで `/user/usage/1/edit` にアクセスすると編集画面が表示される
- [x] userで `/user/usage/1/delete` にアクセスすると削除画面が表示される
- [x] userで `/user/share-amounts` にアクセスするとシェア金額一覧画面が表示される
- [x] 未ログインで `/user/usage/logs` にアクセスするとログイン画面へ遷移して「ログインしてください」が表示される
- [x] adminで `/user/usage/logs` にアクセスするとフラッシュメッセージなしで `/admin/top` に戻る
- [x] 使用記録一覧画面の家アイコンを押すと `/` 経由で適切なトップ画面へ戻る
- [ ] success flash または danger flash が使用記録一覧画面に表示される

-------------------------

## ■ 一般ユーザー：使用記録一覧 Step 2
実施：2026-03-22

### 【実装確認】
- [x] 使用記録一覧画面で、ログイン中ユーザー自身の使用記録だけを取得している
- [x] `device_usage_logs.deleted_at IS NULL` の記録だけを対象にしている
- [x] 自己所有機器に紐づく記録だけを対象にしている
- [x] 記録を `start_time` の新しい順で取得している
- [x] 最新の `finalized_bills.period_end` を基準に未確定期間を判定している
- [x] `finalized_bills` に1件以上ある場合、最新 `period_end` を `Asia/Tokyo` に変換したうえで翌日 `00:00` を未確定期間の開始日時としている
- [x] `finalized_bills` に1件以上ある場合、未確定期間の開始日時以降の記録だけを一覧対象にしている
- [x] `finalized_bills` にレコードが1件も存在しない場合、全期間を対象にしている
- [x] `finalized_bills` にレコードが1件も存在しない場合、開始表示文言を「初回利用記録」にしている
- [x] `finalized_bills` に1件以上ある場合、上部表示エリアに最新 `period_end` の翌日の日付を `yyyy/mm/dd` 形式で表示している
- [x] 一覧対象が0件の場合はカードを表示せず、`表示できる使用記録はありません` を表示している
- [x] この段階では編集・削除の本処理を実装していない

---

### 【手動テスト】
- [x] userで `/user/usage/logs` にアクセスすると、自分の使用記録一覧画面が表示される
- [x] 使用記録が0件の場合、「表示できる使用記録はありません」と表示される
- [x] `finalized_bills` が1件以上ある状態で、上部表示エリアに最新 `period_end` の翌日の日付が表示される
- [x] 未確定期間より前の記録が一覧に表示されない
- [x] 新しい記録ほど上に表示される

-------------------------

## ■ 一般ユーザー：使用記録一覧 Step 3
実施：2026-03-22

### 【実装確認】
- [x] 機器絞り込みを GET パラメータで実装している
- [x] 自己所有機器のみを絞り込み対象にしている
- [x] JST変換して日時を表示している
- [x] 曜日を日本語1文字で表示している
- [x] 概算料金を正しく算出している
- [x] 運転中記録では概算料金を表示しない
- [x] モーダルにも同じ表示データを渡している
- [x] 一覧対象の概算料金合計を算出している

---

### 【手動テスト】
- [x] 機器絞り込みが正常に動く
- [x] 日時が日本時間で表示される（ズレない）
- [x] 曜日が正しく表示される
- [x] 終了済み記録にだけ金額が表示される
- [x] 運転中記録に金額が表示されない
- [x] 合計金額が各カードの合計と一致する

-------------------------

## ■ 一般ユーザー：使用記録一覧 Step 4（モーダル動作と導線接続）
実施：2026-03-22

### 【実装確認】
- [x] `.modal-overlay.active` でモーダル表示できるCSSがある
- [x] チェックアイコン押下で対応するモーダルを開くJavaScriptがある
- [x] 「やめる」ボタン押下でモーダルを閉じるJavaScriptがある
- [x] モーダル枠外クリックでモーダルを閉じるJavaScriptがある
- [x] 「シェア金額一覧を見る」ボタンが `user_share_amounts` に接続されている

---

### 【手動テスト】
- [x] 各記録カードのチェックアイコンを押すと対応する編集削除モーダルが開く
- [x] モーダル内の「やめる」を押すとモーダルが閉じる
- [x] モーダルの枠外を押すとモーダルが閉じる
- [x] モーダル内をクリックしても勝手に閉じない
- [x] 「シェア金額一覧を見る」を押すとシェア金額一覧画面へ遷移する

-------------------------

## ■ 一般ユーザー：編集画面（Step 1）
実施：2026-03-22

### 【実装確認】
- [x] `/user/usage/<int:usage_log_id>/edit` の GET ルートがある
- [x] login_required（ログイン必須）が付いている
- [x] 一般ユーザー以外はロール別トップへ戻す分岐がある
- [x] 編集対象を「自分の機器の記録」に絞って取得している
- [x] 論理削除済み記録を除外している
- [x] 確定済み期間の記録を除外している
- [x] 条件外の記録は flash なしで `/user/usage/logs` へリダイレクトする
- [x] `form_data` に `device_id / start_time / end_time` の初期値を詰めている
- [x] 開始日時・停止日時の初期値を JST の `datetime-local` 形式に変換している
- [x] 編集画面テンプレートへ `devices` と `form_data` を渡している
- [x] 「やめる」ボタンが `/user/usage/logs` に接続されている

---

### 【手動テスト】
- [x] 一般ユーザーで自分の編集画面を開くと、機器・開始日時・停止日時の初期値が入っている
- [x] 一般ユーザーで編集画面を開くと、自分の所有機器だけが選択肢に表示される
- [x] 「やめる」ボタン押下で使用記録一覧へ戻る
- [x] 管理者アカウントで編集URLを直接開くと、管理者トップ側へ戻る

-------------------------

## ■ 一般ユーザー：編集画面の更新処理（Step 2）
実施：2026-03-22

### 【実装確認】
- [x] `/user/usage/<int:usage_log_id>/edit` が GET / POST の両方に対応している
- [x] login_required（ログイン必須）が付いている
- [x] 一般ユーザー以外はロール別トップへ戻す分岐がある
- [x] 編集対象を「自分の機器の記録」「未削除」「未確定期間」に絞って取得している
- [x] 条件外の記録は `/user/usage/logs` にリダイレクトする
- [x] POSTで `device_id / start_time / end_time` を受け取っている
- [x] 対象レコードをPOST時にも再取得している
- [x] 値を更新して commit している
- [x] 更新成功時に一覧画面へリダイレクトしている
- [x] success flash メッセージが出る

---

### 【手動テスト】
- [x] 編集画面で値を変更して「更新する」を押すと、使用記録一覧へ戻り「記録を更新しました」が表示される
- [x] 編集画面で値を変更して更新すると、一覧画面に変更内容が反映されている
- [x] 編集画面で何も変更せず「更新する」を押してもエラーにならず一覧へ戻る
- [x] 「やめる」ボタン押下で使用記録一覧へ戻る
- [x] 他人の記録URLを直接開いた場合は一覧へ戻る（GET側の挙動確認）

-------------------------

## ■ 一般ユーザー：編集画面のバリデーション（Step 3）
実施：2026-03-22

### 【実装確認】
- [x] device_id の必須チェックがある
- [x] start_time の必須チェックがある
- [x] device_id の数値変換チェックがある
- [x] device_id の所有権チェックがある（ログインユーザーの機器のみ）
- [x] datetime-local の形式チェックがある
- [x] start_time をUTCに変換している
- [x] end_time をUTCに変換している（入力時のみ）
- [x] 確定済み期間の記録チェックがある
- [x] 未来日時チェックがある（開始・停止）
- [x] start_time < end_time のチェックがある
- [x] 未終了記録の重複チェックがある（自分自身除外）
- [x] エラー時に danger flash を表示している
- [x] エラー時に入力値を保持して再表示している

---

### 【手動テスト】
- [x] 開始日時を未入力で更新するとエラーになる
- [x] 不正な日時形式を入力するとエラーになる
- [x] 開始日時 > 停止日時で更新するとエラーになる
- [x] 未来日時を入力するとエラーになる
- [x] 未終了記録がある状態で停止日時なし更新するとエラーになる
- [x] 確定済み期間の日時で更新するとエラーになる
- [x] 正常な入力で更新すると一覧へ戻る

-------------------------

## ■ 一般ユーザー：使用記録編集機能
実施：2026-03-22

### 【実装確認】
- [x] `/user/usage/<int:usage_log_id>/edit` が GET / POST の両方に対応している
- [x] login_required（ログイン必須）が付いている
- [x] 一般ユーザー以外はロール別トップへ戻す分岐がある
- [x] 編集対象を「自分の機器の記録」「未削除」「未確定期間」に絞って取得している
- [x] 条件外の記録は flash なしで `/user/usage/logs` にリダイレクトする
- [x] 編集画面に初期値を表示する `form_data` を渡している
- [x] 日時の初期値を JST の `datetime-local` 形式に変換している
- [x] `device_id / start_time / end_time` をPOSTで受け取っている
- [x] 使用機器の必須チェックがある
- [x] 開始日時の必須チェックがある
- [x] 使用機器IDの数値変換チェックがある
- [x] 使用機器がログイン中ユーザー所有かどうかのチェックがある
- [x] `datetime-local` の形式チェックがある
- [x] 未来日時チェックがある
- [x] `start_time < end_time` のチェックがある
- [x] 停止日時なし更新時の未終了記録重複チェックがある（自分自身除外）
- [x] 確定済み期間の記録を編集不可にしている
- [x] 更新成功時は一覧画面へリダイレクトし、success flash を表示する
- [x] 更新失敗時は編集画面を再表示し、danger flash を表示する
- [x] 例外発生時に rollback とログ出力を行っている
- [x] 編集画面テンプレートで success / danger のカテゴリ表示に対応している
- [x] 「やめる」ボタンが `/user/usage/logs` に接続されている

---

### 【手動テスト】
- [x] 自分の記録の編集画面を開くと、機器・開始日時・停止日時の初期値が正しく入っている
- [x] 機器を変更して更新すると、一覧へ戻って「記録を更新しました」が表示される
- [x] 開始日時を未入力で更新すると、編集画面のままでエラーになる
- [x] 開始日時より前の停止日時を入れて更新すると、編集画面のままでエラーになる
- [x] 未来日時を入れて更新すると、編集画面のままでエラーになる
- [x] 停止日時を空にして更新できる（他に未終了記録がない場合）
- [x] 他に未終了記録がある状態で停止日時を空にして更新すると、編集画面のままでエラーになる
- [x] 他人の記録URLを直接開くと、使用記録一覧へ戻る
- [x] 「やめる」ボタン押下で使用記録一覧へ戻る
- [x] 正常更新時は一覧画面側で success flash が表示される
- [x] 異常更新時は編集画面側で danger flash が赤系で表示される

-------------------------

## ■ 一般ユーザー：使用記録削除画面（Step 1）
実施：2026-03-22

### 【実装確認】
- [x] `/user/usage/<int:usage_log_id>/delete` の GET ルートがある
- [x] login_required（ログイン必須）が付いている
- [x] 一般ユーザー以外はロール別トップへ戻す分岐がある
- [x] 削除対象を「自分の機器の記録」「未削除」「未確定期間」に絞って取得している
- [x] 条件外の記録は flash なしで `/user/usage/logs` にリダイレクトする
- [x] 削除画面に表示する記録データを app.py 側で組み立てている
- [x] 機器名を表示している
- [x] 開始日時を日本時間表示で出している
- [x] 停止日時を日本時間表示で出している
- [x] 停止日時がない場合は「運転中」と表示する分岐がある
- [x] 概算料金の表示データを既存ロジックで作っている
- [x] 「やめる」ボタンが `/user/usage/logs` に接続されている

---

### 【手動テスト】
- [x] 自分の記録の削除画面を開くと、機器名・開始日時・停止日時が表示される
- [x] 終了済み記録を開くと、概算料金が表示される
- [x] 運転中の記録を開くと、「運転中」と表示される
- [x] 「やめる」ボタン押下で使用記録一覧へ戻る
- [x] 他人の記録URLを直接開くと、使用記録一覧へ戻る

-------------------------

## ■ 一般ユーザー：使用記録の論理削除（Step 2）
実施：2026-03-22

### 【実装確認】
- [x] `/user/usage/<int:usage_log_id>/delete` が GET / POST の両方に対応している
- [x] login_required（ログイン必須）が付いている
- [x] 一般ユーザー以外はロール別トップへ戻す分岐がある
- [x] 削除対象を「自分の機器の記録」「未削除」「未確定期間」に絞って取得している
- [x] 条件外の記録は flash なしで `/user/usage/logs` にリダイレクトする
- [x] GET / POST で同じ条件の対象取得処理を使っている
- [x] 削除処理で物理削除ではなく `deleted_at` に日時を保存している
- [x] `deleted_at` には UTC の現在日時を入れている
- [x] 削除成功時に一覧画面へリダイレクトしている
- [x] 削除成功時に success flash メッセージを出している
- [x] 削除画面に form がある
- [x] CSRF トークンがある
- [x] 「やめる」ボタンが `/user/usage/logs` に接続されている

---

### 【手動テスト】
- [x] 削除画面で「削除する」を押すと、使用記録一覧へ戻り「記録を削除しました」が表示される
- [x] 削除した記録が使用記録一覧から消える
- [x] 削除済みの記録URLをもう一度直接開いた場合は、使用記録一覧へ戻る
- [x] 「やめる」ボタン押下で使用記録一覧へ戻る

-------------------------

## ■ 一般ユーザー：使用記録の論理削除（Step 3）
実施：2026-03-22

### 【実装確認】
- [x] `/user/usage/<int:usage_log_id>/delete` が GET / POST の両方に対応している
- [x] login_required（ログイン必須）が付いている
- [x] 一般ユーザー以外はロール別トップへ戻す分岐がある
- [x] 削除対象を「自分の機器の記録」「未削除」「未確定期間」に絞って取得している
- [x] 条件外の記録は flash なしで `/user/usage/logs` にリダイレクトする
- [x] GET / POST で同じ条件の対象取得処理を使っている
- [x] 物理削除ではなく `deleted_at` に日時を保存している
- [x] `deleted_at` には UTC の現在日時を入れている
- [x] 削除成功時に一覧画面へリダイレクトし、success flash を表示している
- [x] 削除失敗時に rollback を行っている
- [x] 削除失敗時にログ出力を行っている
- [x] 削除失敗時に削除画面を再表示している
- [x] 削除画面に flash 表示枠がある
- [x] 削除画面の flash が `with_categories=True` でカテゴリ表示対応している
- [x] success は緑系、danger は赤系で表示できる構造になっている
- [x] 削除画面に form がある
- [x] CSRF トークンがある
- [x] 「やめる」ボタンが `/user/usage/logs` に接続されている
- [x] 削除画面の表示内容が一覧モーダルと同じ考え方で組み立てられている

---

### 【手動テスト】
- [x] 削除画面で「削除する」を押すと、使用記録一覧へ戻り「記録を削除しました」が表示される
- [x] 削除した記録が使用記録一覧から消える
- [x] 他人の記録URLを直接開いた場合は、使用記録一覧へ戻る
- [x] 削除済みの記録URLをもう一度直接開いた場合は、使用記録一覧へ戻る
- [x] 「やめる」ボタン押下で使用記録一覧へ戻る
- [x] 削除失敗時に、削除画面上で danger flash が赤系で表示される

-------------------------

## ■ 管理者トップ画面と関連最小画面（Step 1）
実施：2026-03-23

### 【実装確認】
- [x] 管理者トップ画面の既存ルートが `login_required` + `admin_required` で保護されている
- [x] 電気料金確定画面の GET ルートが追加されている
- [x] 確定済みの電気料金一覧画面の GET ルートが追加されている
- [x] 電気料金確定画面が管理者限定画面になっている
- [x] 確定済みの電気料金一覧画面が管理者限定画面になっている
- [x] `admin_top.html` を既存テンプレートから最小変更で使っている
- [x] 電気料金確定画面モックがテンプレート化されている
- [x] 確定済みの電気料金一覧画面モックがテンプレート化されている
- [x] 管理者トップから「電気料金の確定」へ遷移する導線がある
- [x] 管理者トップから「確定済の電気料金」へ遷移する導線がある
- [x] 電気料金確定画面に HOME 導線がある
- [x] 確定済みの電気料金一覧画面に HOME 導線がある
- [x] 各画面にログアウト導線があり、POST + CSRF トークンになっている
- [x] 既存のシェアメンバー画面・機器画面への導線を壊していない

---

### 【手動テスト】
- [x] 管理者でログインすると管理者トップ画面が開ける
- [x] 管理者トップの「電気料金の確定」メニューから電気料金確定画面へ遷移できる
- [x] 管理者トップの「確定済の電気料金」メニューから確定済みの電気料金一覧画面へ遷移できる
- [x] 電気料金確定画面の HOME から管理者トップへ戻れる
- [x] 確定済みの電気料金一覧画面の HOME から管理者トップへ戻れる
- [x] 電気料金確定画面でログアウトするとログイン画面へ戻る
- [x] 確定済みの電気料金一覧画面でログアウトするとログイン画面へ戻る
- [x] 一般ユーザーで `/admin/top` を直接開くと管理者トップに入れない
- [x] 一般ユーザーで `/admin/bills/confirm` を直接開くと管理者画面に入れない
- [x] 一般ユーザーで `/admin/bills` を直接開くと管理者画面に入れない

-------------------------

## ■ 管理者トップ画面の表示データ実装（Step 2）
実施：2026-03-23

### 【実装確認】
- [x] `admin_top()` で最新の `finalized_bills` を取得している
- [x] 未確定期間開始日を算出している
- [x] 電気料金確定ショートカットカード用の表示データを作成している
- [x] `app_settings.estimated_unit_price` を取得している
- [x] 直近3件の確定単価平均値（表示用）を算出している
- [x] `role='user'` のメンバー一覧を取得している
- [x] メンバーごとの機器使用料金目安カード用データを作成している
- [x] 料金目安カードは終了済み記録のみを集計対象にしている
- [x] 運転中記録を料金目安カードの集計に含めていない
- [x] メンバーカードの並び順を「最新 `start_time` が新しい順」にしている
- [x] 未確定記録一覧データを作成している
- [x] 未確定記録一覧では `deleted_at` の有無にかかわらず取得している
- [x] `deleted_at` がある記録に「削除済」状態を付けている
- [x] `end_time` が NULL の記録に「運転中」状態を付けている
- [x] 運転中記録の使用時間・料金目安を `-` 表示にしている
- [x] `admin_top.html` にショートカットカードの表示が入っている
- [x] `admin_top.html` に仮単価カードの表示が入っている
- [x] `admin_top.html` にメンバーカードの表示が入っている
- [x] `admin_top.html` に未確定記録一覧の表示が入っている
- [x] メンバー絞り込みプルダウンの選択肢を `role='user'` から表示している

---

### 【手動テスト】
- [x] 管理者トップ画面で、ショートカットカードの最終確定日・利用期間・未確定開始日が表示される
- [x] 現在の仮単価カードに、現在の仮単価が表示される
- [x] 直近3件の確定単価平均値が表示される（データがある場合）
- [x] メンバーごとの料金目安カードに、一般ユーザーのみ表示される
- [x] メンバーカードの並び順が、最近動いたメンバーほど左側になる
- [x] 未確定記録一覧に、未確定期間内の記録が表示される
- [x] 運転中記録がある場合、使用時間・料金目安が `-`、状態が「運転中」と表示される
- [x] 論理削除済み記録がある場合、状態が「削除済」と表示される
- [x] メンバー絞り込みプルダウンに admin が表示されない
- [x] ユーザー新規追加で選んだテーマカラーが、管理者トップのメンバーカード背景色に反映される

-------------------------

## ■ 管理者トップ画面の仮単価更新（Step 3）
実施：2026-03-23

### 【実装確認】
- [x] `/admin/top` が GET / POST の両方に対応している
- [x] 管理者トップ画面が `login_required` + `admin_required` で保護されている
- [x] 仮単価カードが `<form method="post">` 化されている
- [x] CSRF トークンがある
- [x] 保存ボタンが submit 化されている
- [x] ラジオボタンに `estimated_price_mode` の name が付いている
- [x] 入力欄に `estimated_unit_price` の name が付いている
- [x] `estimated_price_mode=manual` の場合、入力値で更新する処理がある
- [x] `estimated_price_mode=latest_three_average` の場合、最新3件の確定単価平均で更新する処理がある
- [x] `finalized_bills` が0件のとき、直近3件平均での更新を禁止している
- [x] `app_settings` がない場合は新規作成する
- [x] `app_settings` がある場合は先頭1件を更新する
- [x] 保存成功時に success flash を表示してリダイレクトしている
- [x] 現在の仮単価入力欄に表示用の初期値を渡している

---

### 【手動テスト】
- [x] 「任意の金額」を選んで値を入力し、保存すると現在の仮単価が更新される
- [x] 「直近3件の確定単価平均値」を選んで保存すると、現在の仮単価が平均値に更新される
- [x] 保存後に管理者トップへ戻る

※下記2件はDBデータの調整が必要なため、今回は未実施
- [ ] `finalized_bills` が0件の状態で「直近3件の確定単価平均値」を選んで保存すると、更新されずエラーメッセージが出る
- [ ] `app_settings` が未作成の状態でも、初回保存で仮単価が作成される

-------------------------

## ■ 管理者トップ画面の仮単価更新バリデーション（Step 4）
実施：2026-03-23

### 【実装確認】
- [x] 任意の金額が未入力の場合に danger flash を出す
- [x] 任意の金額が小数第1位までの数値かをサーバー側でチェックしている
- [x] 任意の金額が 0 より大きいかをサーバー側でチェックしている
- [x] 不正な `estimated_price_mode` を danger flash で弾いている
- [x] `finalized_bills` が0件のとき、直近3件平均での更新を禁止している
- [x] `db.session.commit()` を try / except で囲っている
- [x] DB更新失敗時に rollback を行っている
- [x] DB更新失敗時にログ出力を行っている
- [x] DB更新失敗時に danger flash を表示している

---

### 【手動テスト】
- [x] 「任意の金額」を空欄で保存するとエラーメッセージが出る
- [x] 「任意の金額」に `12.34` を入れて保存するとエラーメッセージが出る
- [x] 「任意の金額」に `0` を入れて保存するとエラーメッセージが出る
- [x] 「任意の金額」に負の値を入れて保存するとエラーメッセージが出る
- [x] 「任意の金額」に `12.3` を入れて保存すると更新できる
- [x] 「任意の金額」に整数（例: `12`）を入れて保存すると更新できる
- [x] `finalized_bills` がある状態で「直近3件の確定単価平均値」を選んで保存すると更新できる
- [x] 保存成功後、画面に戻って値が更新されている

-------------------------

## ■ 管理者トップ画面の未確定記録一覧絞り込み（Step 5）
実施：2026-03-23

### 【実装確認】
- [x] 未確定記録一覧にメンバー絞り込みがある
- [x] メンバー絞り込みの選択肢は `role='user'` のみになっている
- [x] 不正な member_id をサーバー側で弾いている
- [x] 未確定記録一覧に期間絞り込みがある
- [x] 期間絞り込みは `start_time` を基準にしている
- [x] 開始日・終了日を日本時間の日付で受け取り、UTC に変換して比較している
- [x] 終了日を当日 23:59:59 まで含めている
- [x] 未確定期間開始日より前の日付入力をサーバー側で弾いている
- [x] 未来日の入力をサーバー側で弾いている
- [x] 開始日 > 終了日をサーバー側で弾いている
- [x] 未確定記録一覧で deleted_at の有無にかかわらず記録を取得している
- [x] deleted_at がある記録に「削除済」バッジを表示している
- [x] end_time が NULL の記録に「運転中」バッジを表示している
- [x] 運転中記録の使用時間・料金目安を `-` 表示にしている
- [x] flash が `with_categories=True` でカテゴリ表示対応している
- [x] success は緑系、danger は赤系で表示できる
- [x] メンバー絞り込みの初期値が「すべてのメンバー」になっている
- [x] value="" の場合は全件表示になる（既存ロジック活用）
- [x] 絞り込みボタンが正常に機能する
- [x] 「リセット」ボタンが追加されている
- [x] リセットで GETパラメータなしの状態に戻る
- [x] メンバー・期間の絞り込みが両方解除される
- [x] リセットボタンのデザインが既存ボタンと統一されている

---

### 【手動テスト】
- [x] メンバー絞り込みで特定ユーザーを選ぶと、そのユーザーの未確定記録だけ表示される
- [x] 期間絞り込みで開始日・終了日を指定すると、その範囲の未確定記録だけ表示される
- [x] 開始日を終了日より後にすると、danger flash が表示される
- [x] 未確定期間開始日より前の日付を入れると、danger flash が表示される
- [x] 未来日を入れると、danger flash が表示される
- [x] 仮単価更新の success flash は緑系、エラー時の danger flash は赤系で表示される
- [x] 「すべてのメンバー」で絞り込みすると全件表示になる
- [x] 特定メンバーで絞り込み → そのユーザーだけ表示される
- [x] 期間絞り込み後に「リセット」を押すと全件表示に戻る
- [x] メンバー＋期間で絞り込み後に「リセット」で両方解除される
- [x] リセット後、初期状態（全件表示）に戻る

-------------------------

## ■ 管理者：電気料金確定画面（Step 1 GET表示強化）
実施：2026-03-24

### 【実装確認】
- [x] `/admin/bills/confirm` に login_required（ログイン必須） + admin_required（管理者限定）が付いている
- [x] 最新の確定済み電気料金を `period_end` 降順で1件取得している
- [x] 通常時は開始日を「最新 period_end の翌日」で固定する分岐がある
- [x] 初回確定時は開始日を手入力に切り替える分岐がある
- [x] 最新確定情報の右上表示をテンプレートへ渡している
- [x] 未入力時の確定単価表示が「- - 円」になっている
- [x] プレビュー対象が `role='user'` のみになっている
- [x] 未入力時のカード並び順が `created_at ASC, id ASC` になっている
- [x] 未入力時のカード金額表示が「- - - - -円」になっている
- [x] 未入力時のカード色が `#f2f2f2` になっている
- [x] finalized_bills が存在する場合、日付付き文言が表示される
- [x] finalized_bills が0件の場合、固定文言が表示される
- [x] テンプレート側で条件分岐せず、app.py で文言を生成している

---

### 【手動テスト】
- [x] 管理者で `/admin/bills/confirm` を開くと、電気料金確定画面が表示される
- [x] サイドメニューの「電気料金の確定」がアクティブ表示になっている
- [x] 最新の確定済みデータがある状態で、右上に「最終確定日」と「利用期間」が表示される
- [x] 最新の確定済みデータがある状態で、開始日が固定表示（変更不可）になっている
- [x] プレビューカードに一般ユーザーの名前が表示される
- [x] 未入力時のプレビューカード金額がすべて「- - - - -円」になっている
- [x] 未入力時の確定単価表示が「- - 円」になっている
- [x] 入力欄に文字を打ったとき、画面エラーや見た目崩れが起きない

※下記1件はDB前提データの調整が必要なため、今回は未実施
- [ ] 初回確定状態（finalized_bills が0件）で、開始日が手入力できる

-------------------------

## ■ 管理者：電気料金確定画面（Step 2 入力受け取り・プレビュー・確認モーダル）
実施：2026-03-24

### 【実装確認】
- [x] `/admin/bills/confirm` が GET / POST 対応になっている
- [x] `login_required` と `admin_required` が維持されている
- [x] `<form method="post">` と `csrf_token` が追加されている
- [x] 終了日、請求総額、基本料金、使用量、初回時の開始日を POST で受け取る実装がある
- [x] 必須チェック、数値チェック、`base_fee <= billing_amount`、開始日/終了日順序の土台がある
- [x] JST日付をUTC aware datetimeへ変換する補助関数が追加されている
- [x] 確定単価を小数第1位で表示する実装がある
- [x] プレビュー計算用の共通関数が追加されている
- [x] プレビュー集計が `deleted_at IS NULL`、`end_time IS NOT NULL`、`start_time` 基準、`role='user'` で行われている
- [x] 円単位の丸めと差額調整の実装がある
- [x] 保存処理（finalized_bills / finalized_bill_members への書き込み）はまだ入っていない
- [x] 画面下の「この期間の料金を確定する」ボタンが `type="button"` になっている
- [x] モーダル内の「この内容で確定する」ボタンだけが submit する構造になっている
- [x] モーダル内に利用期間、請求総額、基本料金、使用量、確定単価の表示欄がある
- [x] モーダルのキャンセル、×ボタン、外側クリックで閉じるJSがある
- [x] 入力保持して同画面再表示する実装がある

---

### 【手動テスト】
- [x] 管理者で電気料金確定画面を開ける
- [x] 通常時、開始日が固定表示で変更できない
- [x] 画面下の「この期間の料金を確定する」ボタンで確認モーダルが開く
- [x] モーダル内に利用期間・請求総額・基本料金・使用量・確定単価が表示される
- [x] モーダルの「キャンセル」で閉じる
- [x] モーダルの「×」で閉じる
- [x] モーダル外クリックで閉じる
- [x] モーダルを閉じても入力値が消えない
- [x] 必須項目が明らかに不足している状態では、モーダルが開かない

【自動プレビューに修正後に追加】
- [x] 有効な値（終了日・請求総額・基本料金・使用量）をすべて入力すると、確定単価が自動で表示される
- [x] 有効な値をすべて入力すると、リロードせずに右側プレビューの金額が自動で表示・更新される
- [x] プレビュー計算後、カード色が未入力時のグレーから各メンバー色に自動で切り替わる

-------------------------

## ■ 管理者：電気料金確定画面（Step 3 バリデーション強化）
実施：2026-03-24

### 【実装確認】
- [x] 初回確定時の開始日必須チェックがある
- [x] 初回確定時の開始日未来日禁止チェックがある
- [x] 終了日の未来日禁止チェックがある
- [x] 基本料金が請求総額を上回る入力を禁止している
- [x] 終了日が開始日より前になる入力を禁止している
- [x] 通常時の開始日固定をサーバー側で再検証している
- [x] 期間飛ばし禁止のチェックがある
- [x] 同一期間の二重確定禁止チェックがある
- [x] 対象期間に運転中記録がある場合の確定禁止チェックがある
- [x] 対象期間に終了済み記録がない場合は確定不可になる
- [x] 請求総額が機器使用料金合計を下回る場合の確定禁止チェックがある
- [x] 自動プレビューAPIとPOST再表示が同じ共通関数を使う構造になっている

---

### 【手動テスト】

- [x] 終了日に未来日を入れるとエラーが出る
- [x] 終了日を開始日より前にするとエラーが出る
- [x] 基本料金を請求総額より大きくするとエラーが出る
- [x] 使用量を0以下にするとエラーが出る
- [x] 対象期間に運転中の記録がある場合、エラーが出る
- [x] 請求総額が機器使用料金合計より小さい場合、エラーが出る
- [x] 正常な入力では自動プレビューが引き続き表示される

※下記2件はDB前提データの調整が必要なため、今回は未実施
- [ ] 初回確定時、開始日を未入力にするとエラーが出る
- [ ] 初回確定時、開始日に未来日を入れるとエラーが出る

-------------------------

## ■ 管理者：電気料金確定処理の保存実装
実施：2026-03-24

### 【実装確認】
- [x] 確認モーダル経由時のみ保存処理が動くサーバー側チェックがある
- [x] hidden の `is_confirmed_by_modal` を使った確認済みフラグがある
- [x] `finalized_bills` を保存する処理がある
- [x] `finalized_bill_members` を保存する処理がある
- [x] `finalized_bills` 保存後に `flush()` でIDを取得して内訳保存している
- [x] 保存失敗時に `rollback()` する処理がある
- [x] 保存成功時に `/admin/bills` へリダイレクトする
- [x] 保存成功時に「yyyy/mm/dd～yyyy/mm/dd利用分の電気料金が確定しました」の success flash を出す
- [x] 表示用プレビューと保存用データが同じ `save_payload` を使う構造になっている
- [x] `/admin/bills` がDBの確定データを使って最小動的表示される
- [x] `/admin/bills` にデータ未登録時のプレースホルダー表示がある

---

### 【手動テスト】
- [x] 正常な入力で確認モーダルから確定すると、`/admin/bills` に遷移する
- [x] 確定後、success flash に対象期間が表示される
- [x] 確定後、`/admin/bills` の最新請求欄に請求総額と単価が表示される
- [x] 確定後、`/admin/bills` のメンバーごとのシェア金額が表示される
- [x] 確定後、`/admin/bills` の履歴テーブルに今回の期間が追加される
- [x] 確認モーダルを開かずに Enter キー送信などをしても、保存されない
- [x] 確定後、一般ユーザーの使用記録一覧で今回確定した期間の記録が未確定一覧から外れる

-------------------------

## ■ 管理者：確定済み電気料金一覧画面の詳細切り替え
実施：2026-03-24

### 【実装確認】
- [x] `/admin/bills` の履歴行ごとに `bill_id` を持たせている
- [x] 履歴行ごとの詳細表示用データを app.py でまとめてテンプレートへ渡している
- [x] 詳細表示エリアの確定日・利用分・請求総額・確定単価が切り替わる構造になっている
- [x] メンバーごとのシェア金額カードが履歴行ごとに切り替わる構造になっている
- [x] 初期表示が最新行（先頭行）になるようにしている
- [x] 選択中の行に `selected-row` を付ける実装がある
- [x] finalized_bills がない場合のデフォルト詳細データがある
- [x] finalized_bill_members がない／取得できない場合に未計算カード表示へフォールバックする実装がある
- [x] 未計算カードが confirm 画面未計算時と同様に、グレー・名前表示・金額 `- - - - -円` になる構造がある

---

### 【手動テスト】
- [x] `/admin/bills` を開くと、最新の確定履歴の詳細が初期表示される
- [x] 履歴テーブルの別の行をクリックすると、詳細表示エリアの内容が切り替わる
- [x] 行クリックで、選択中の行の見た目が切り替わる
- [x] 行クリックで、メンバーカードのシェア金額・機器使用分・割り勘分が切り替わる
- [x] カードの並び順がシェア金額の多い順のまま表示される

※下記2件はDB前提データの調整が必要なため、今回は未実施
- [ ] レコードがない状態でも画面が壊れず、未計算カード表示になる
- [ ] レコードがない状態でも画面が壊れず、履歴テーブルが空用の表示になる

-------------------------

## ■ 一般ユーザー：シェア金額一覧・詳細画面（Step 1）
実施：2026-03-25

### 【実装確認】
- [x] シェア金額一覧画面の GET ルートが追加されている
- [x] シェア金額詳細画面の GET ルートが追加されている
- [x] シェア金額一覧画面に login_required（ログイン必須）が付いている
- [x] シェア金額詳細画面に login_required（ログイン必須）が付いている
- [x] シェア金額一覧画面で user ロール以外を弾く分岐がある
- [x] シェア金額詳細画面で user ロール以外を弾く分岐がある
- [x] シェア金額一覧画面の家アイコンが「/」につながっている
- [x] シェア金額詳細画面の家アイコンが「/」につながっている
- [x] シェア金額一覧画面の「使用記録一覧に戻る」ボタンが接続されている
- [x] シェア金額詳細画面の「シェア金額一覧に戻る」ボタンが接続されている
- [x] 最新カードはカード全体クリックで詳細へ遷移する実装になっている
- [x] 履歴カードはチェックアイコンのみクリックで詳細へ遷移する実装になっている
- [x] 一覧画面に空状態用メッセージのテンプレート分岐がある
- [x] 一覧画面に Step 1 用のダミー表示データが渡されている
- [x] 詳細画面に Step 1 用のダミー表示データが渡されている

---

### 【手動テスト】
- [x] 使用記録一覧画面の「シェア金額一覧を見る」から一覧画面へ遷移できる
- [x] シェア金額一覧画面の家アイコン押下で「/」へ遷移できる
- [x] シェア金額一覧画面の最新カード押下で詳細画面へ遷移できる
- [x] シェア金額一覧画面の履歴カード右側チェックアイコン押下で詳細画面へ遷移できる
- [x] シェア金額一覧画面の「使用記録一覧に戻る」ボタンで使用記録一覧へ戻れる
- [x] シェア金額詳細画面の家アイコン押下で「/」へ遷移できる
- [x] シェア金額詳細画面の「シェア金額一覧に戻る」ボタンで一覧へ戻れる
- [x] シェア金額一覧画面の見た目がモックから大きく崩れていない
- [x] シェア金額詳細画面の見た目がモックから大きく崩れていない

-------------------------

## ■ 一般ユーザー：シェア金額一覧画面（Step 2）
実施：2026-03-25

### 【実装確認】
- [x] シェア金額一覧画面の GET ルートに login_required（ログイン必須）が付いている
- [x] シェア金額一覧画面で user ロール以外を弾く分岐がある
- [x] finalized_bills を period_end 降順、id 降順で取得している
- [x] ログイン中ユーザーの finalized_bill_members を取得している
- [x] finalized_bill_id ごとの share_amount を引ける辞書に整形している
- [x] 一覧表示用の bill_cards を作成している
- [x] 各カードに利用期間表示を入れている
- [x] 各カードにログイン中ユーザーのシェア金額表示を入れている
- [x] ユーザー内訳が欠損している場合は「- 円」で安全表示する処理がある
- [x] 先頭1件を latest_record、2件目以降を history_records に分けている
- [x] 表示対象カードが0件のときは空状態として扱う処理がある
- [x] 空状態では一覧下部に指定メッセージを表示するテンプレート分岐がある
- [x] 空状態でも「使用記録一覧に戻る」ボタンが表示される
- [x] 最新カードはカード全体クリックで詳細へ遷移する実装になっている
- [x] 履歴カードはチェックアイコンのみクリックで詳細へ遷移する実装になっている
- [ ] シェア金額詳細画面はまだダミー表示のままで、対象IDの本取得は未実装
- [ ] 詳細画面の存在確認・認可チェックの本実装は未了

---

### 【手動テスト】
- [x] 使用記録一覧画面の「シェア金額一覧を見る」から一覧画面へ遷移できる
- [x] 確定済みデータがある場合、最新カードが1件だけ一番上に表示される
- [x] 確定済みデータが複数ある場合、2件目以降が履歴カードとして下に表示される
- [x] シェア金額一覧画面の最新カード押下で詳細画面へ遷移できる
- [x] シェア金額一覧画面の履歴カード右側チェックアイコン押下で詳細画面へ遷移できる
- [x] シェア金額一覧画面の家アイコン押下で「/」へ遷移できる
- [x] シェア金額一覧画面の「使用記録一覧に戻る」ボタンで使用記録一覧へ戻れる
- [x] シェア金額一覧画面の見た目がモックから大きく崩れていない

※下記1件はDB前提データの調整が必要なため、今回は未実施
- [ ] データ0件の環境では、空状態メッセージと「使用記録一覧に戻る」ボタンだけが表示される

-------------------------

## ■ 一般ユーザー：シェア金額詳細画面（Step 3）
実施：2026-03-25

### 【実装確認】
- [x] シェア金額詳細画面の GET ルートがある
- [x] シェア金額詳細画面に login_required（ログイン必須）が付いている
- [x] シェア金額詳細画面で user ロール以外を弾く分岐がある
- [x] finalized_bill_id と current_user.id の両方で FinalizedBillMember を取得している
- [x] ログイン中ユーザーの内訳がある請求のみ表示する実装になっている
- [x] FinalizedBillMember から関連する FinalizedBill を取得している
- [x] 対象データが存在しない場合は一覧画面へ安全にリダイレクトする
- [x] 詳細画面に利用期間を表示するデータを渡している
- [x] 詳細画面に確定日を表示するデータを渡している
- [x] 詳細画面にお家全体の電気代を表示するデータを渡している
- [x] 詳細画面にあなたのシェア金額を表示するデータを渡している
- [x] 詳細画面に機器使用分を表示するデータを渡している
- [x] 詳細画面に割り勘分を表示するデータを渡している
- [x] 詳細画面に単価を表示するデータを渡している
- [x] 一覧画面の最新カードはカード全体クリックで詳細へ遷移する
- [x] 一覧画面の履歴カードはチェックアイコンのみクリックで詳細へ遷移する
- [ ] 認可NG時の案内メッセージ表示は未実装（静かに一覧へ戻す実装）
- [ ] 一覧画面では、ログイン中ユーザーの内訳がない請求も「- 円」で表示される可能性がある

---

### 【手動テスト】
- [x] シェア金額一覧画面の最新カード押下で詳細画面へ遷移できる
- [x] シェア金額一覧画面の履歴カード右側チェックアイコン押下で詳細画面へ遷移できる
- [x] 詳細画面に利用期間が表示される
- [x] 詳細画面に確定日が表示される
- [x] 詳細画面にお家全体の電気料金が表示される
- [x] 詳細画面にあなたのシェア金額が表示される
- [x] 詳細画面に機器使用分と割り勘分が表示される
- [x] 詳細画面の「シェア金額一覧に戻る」ボタンで一覧へ戻れる
- [x] 詳細画面の家アイコン押下で「/」へ遷移できる
- [x] 詳細画面の見た目がモックから大きく崩れていない